import React from "react";
import Card from "@mui/material/Card";
import { CardContent } from "@mui/material";

const Blog = () => {
  return (
    <Card>
      <CardContent>This is blog</CardContent>
    </Card>
  );
};

export default Blog;
